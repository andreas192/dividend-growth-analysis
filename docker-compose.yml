services:
  postgres:
    build: .
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: dividend
      POSTGRES_PASSWORD: dividend
      POSTGRES_DB: dividend_growth
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
